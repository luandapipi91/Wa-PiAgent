# 文件预览虚拟滚动 Demo · 使用说明

演示 Wa-Pi 文件预览（`FileViewer`）打开**几 MB 文本**时不卡顿的做法：
把全文按「块」切分，**只渲染可视区域内的块**，上下用「未渲染行数 × 行高」占位撑出完整滚动高度。

- **Demo 文件**：`~/Desktop/fileviewer-virtual-scroll-demo.html`（单文件、零依赖）
- **打开方式**：双击即可（也可拖进浏览器）

> 说明：demo 演示的是**代码/文本分支**的固定行高虚拟滚动（与真实实现同款逻辑）。
> markdown 分支同样是块级虚拟滚动，但按「md 顶层块」切分、块高度不固定（实测后收敛），不在这个 demo 里演示。

---

## 界面速览

```text
┌ 顶栏：总行数 / 估算大小 / 当前 DOM 节点 / 本次渲染耗时 / 不虚拟化时的节点数
├ 工具栏：总行数 ▾   块大小 [200] 行   ☑ 启用虚拟滚动   重新生成   对比：全量渲染（会卡）   恢复虚拟滚动
└ 预览区：带行号与着色、可上下滚动的文件内容
```

---

## 怎么玩

| 操作 | 看什么 |
| --- | --- |
| **总行数** 切到 `200,000 行（约 14 MB）` | 依然秒开、滚动流畅；顶栏「当前 DOM 节点」仍是几千 |
| **块大小** 从 `200` 改到 `5000` | 节点数与渲染耗时上升（每次重建的块变大），但仍在可接受范围 |
| **取消勾选**「启用虚拟滚动」 | 一次性建出全部行 → 节点数暴增、页面明显卡住 |
| **对比：全量渲染（会卡）** | 亲手体验卡顿（>6 万行会先弹确认框）；看完点「恢复虚拟滚动」 |
| 滚动到中段 / 底部 | 「首个渲染行号」跟着变，但节点数基本不变 —— 这就是虚拟滚动 |

> 提示：全量渲染 10 万行以上可能让标签页无响应几十秒，体验完记得点「恢复虚拟滚动」。

---

## 指标含义

| 指标 | 含义 | 怎么看 |
| --- | --- | --- |
| **文件行数** | 当前生成的模拟文件总行数 | 对应真实文件的长度 |
| **估算大小** | 按字符数累加估算 | 对照 kernel 的 5MB 预览上限 |
| **当前 DOM 节点** | 预览区里实际存在的元素数量 | 虚拟滚动下应稳定在**几千**；全量渲染会到**几万～几十万** |
| **本次渲染耗时** | 本次窗口重建花的时间 | 虚拟滚动下通常 < 20 ms（一帧以内） |
| **不虚拟化时的节点数** | 按「每行约 8 个节点」估算 | 用来对比"如果不做虚拟化会有多糟" |

---

## 预期现象（本机 Chrome 实测）

**40,000 行 / 2.89 MB**

| 滚动位置 | DOM 节点 | 渲染耗时 | 首个渲染行号 |
| --- | --- | --- | --- |
| 顶部 | 3,188 | 7.5 ms | 1 |
| 中部 | 4,820 | 11.9 ms | 19,801 |
| 底部 | 3,120 | 9.3 ms | 39,601 |

**同一份 10,000 行，开/关虚拟滚动对比**

| 模式 | DOM 节点 | 耗时 |
| --- | --- | --- |
| 虚拟滚动 | 3,188 | 38 ms |
| 全量渲染 | **78,888** | 197 ms 渲染，但**主线程被占住约 2.2 秒** |
| 切回虚拟滚动 | 3,188 | — |

结论：**节点数只跟视口高度有关，与文件行数基本无关**；全量渲染则随行数线性增长 —— 10k 行就卡 2 秒，40k 行、5MB 的文件会卡到几十秒甚至崩溃。

---

## 与 Wa-Pi 实际实现的关系

Demo 用的是和真实组件**同一套思路**：

| 项 | Demo | Wa-Pi · 代码/文本分支 | Wa-Pi · markdown 分支 |
| --- | --- | --- | --- |
| 切分单位 | 每 200 行一块 | `VIRTUAL_CHUNK_LINES = 200` | `splitMarkdownBlocks()`（顶层块：围栏代码块不切、标题/空行起块、200 行兜底） |
| 窗口计算 | 页面内联 | `computeChunkWindow()` | `computeBlockWindow()` |
| 占位 | 上下 spacer | 固定行高（实测修正） | 块高度实测缓存 + 未实测块估算 |
| 高亮/渲染 | 轻量正则 | Prism（每块独立） | ReactMarkdown（每块独立） |

对应代码：`packages/frontend/src/components/blocks/FileViewer.tsx`
（另有 kernel 侧硬上限：`packages/kernel/src/routes/fs.ts` 的 `MAX_PREVIEW_BYTES = 5MB`，超过直接提示「文件过大」，不进入渲染。）

---

## 常见疑问

**Q：为什么滚动条长度和"全文"一致？**
A：上下各放一个占位元素（高度 = 未渲染内容的估算/实测高度和）。markdown 块高度不固定，实测后会自动收敛。

**Q：为什么节点数不随行数增长？**
A：任何时刻只渲染「视口 + 前后各一块」。行数变多只是让占位变高。

**Q：为什么全量渲染会卡？**
A：每个 token 一个 `<span>`、每行一个 `div`；5MB 文件约 190 万 token ≈ 200 万节点，建树 + 布局都在主线程。

**Q：滚动时会闪白吗？**
A：不会。窗口外各多渲染一块（overscan）。把「块大小」调到 5000 更容易观察这个行为。
