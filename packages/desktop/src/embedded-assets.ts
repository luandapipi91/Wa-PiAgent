// 本文件由 packages/desktop/scripts/build.ts 在打包时临时重新生成为嵌入资源清单
// （各 `import aN from "./embedded/..." with { type: "file" }`）。此空 stub 是入库基线。
export const EMBEDDED_ASSETS: { src: string; dest: string }[] = [];
