// 本文件由 scripts/build.ts 在打包阶段生成（扫描 embed 清单后覆写）。
// 此处为占位 stub，使 main.ts 在 Task 13 真清单注入前能通过 typecheck。
// 不要手动编辑——以 build.ts 输出为准。
export const EMBEDDED_ASSETS: { src: string; dest: string }[] = [];
