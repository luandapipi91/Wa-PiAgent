WA PI Agent 0.2.20 更新内容：

【新增】

- 多模态图片优化：超过单张上限（3.5MB）且 ≤30MB 的图片自动用 bun:image 压缩为 webp 再内联发送，不再直接降级为附件丢弃像素（≤30MB 均不再放弃）

【修复】

- 模型设置里的「图片」开关真正生效（此前仅展示，不改变生成 input）。选择支持视觉的模型后输入图片，LLM 能正常收到像素，不再被降级为 (image omitted)
