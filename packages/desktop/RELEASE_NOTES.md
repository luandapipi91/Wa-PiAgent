WA PI Agent 0.2.24 更新内容：

【修复】

- 内核升级 0.1.2 → 0.1.3：发送前自动压缩阈值从「窗口 − 33K 预留」改为「窗口 85%」更早触发，修复大窗口下 pi 的 token 估算偏低、导致窗口边缘溢出 400 的问题
- 内核更新清单平台化：darwin / win / linux 各用 `kernel-latest-<platform>.json` 清单与平台化文件名 zip，多平台共存互不覆盖，修复同 build 号跨平台覆盖问题

【改进】

- 应用内置内核同步到 0.1.3（与独立发布的内核一致），首次安装即用最新内核
