WA PI Agent 0.2.25 更新内容：

【新增】

- 事件完成提示音改为随机池播放（新增 6 个前 1 秒音效，移除重复的青蛙叫），多事件完成时音效不同更自然

【修复】

- 切换会话时预览归属到对应会话；新建会话页文件树打开的 HTML 预览归属到新建页锚点 sessionId，切走不残留
- 本地预览高亮选择框在屏幕边缘时收敛进视口，不再溢出
- 同一会话同时只允许一条引导中队列
