WA PI Agent 0.2.3 更新内容：

【新增】

- 文件修改清单：每轮对话结束后，回复底部按文件去重展示本轮改动（标注新增/修改），点击文件名展开整文件行级 diff（同一行多次修改自动合并为净变化），点击打开内置预览

【修复】

- 文件预览路径解析：Windows 盘符被误当相对路径
- 安装后打不开（白屏）：kernel 启动时 file-snapshot.ts 未随 seed 同步到 runtime 目录导致启动崩溃，已补全同步清单
