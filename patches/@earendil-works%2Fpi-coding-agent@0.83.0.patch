diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 02c3567ada40890c280a68909d9a013491219615..5bbf73a08a6994eb7e3c1a213bd4a7ff6be0ec16 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -933,6 +933,11 @@ export class AgentSession {
             return true;
         }
         catch (err) {
+            // TUI-only command attempted in RPC mode (ctx.ui.custom unavailable):
+            // degrade gracefully — treat the original text as a normal prompt so it
+            // flows to the LLM like any unknown slash command, instead of erroring.
+            if (err && err.code === "PI_TUI_ONLY")
+                return false;
             // Emit error via extension runner
             this._extensionRunner.emitError({
                 extensionPath: `command:${commandName}`,
diff --git a/dist/modes/rpc/rpc-mode.js b/dist/modes/rpc/rpc-mode.js
index 8567321f20caa900a4f0268d54b2921e6efd1418..73beebeac5a0a2c536fecce63be4912aab32ef6e 100644
--- a/dist/modes/rpc/rpc-mode.js
+++ b/dist/modes/rpc/rpc-mode.js
@@ -148,9 +148,19 @@ export async function runRpcMode(runtimeHost) {
                 title,
             });
         },
-        async custom() {
-            // Custom UI not supported in RPC mode
-            return undefined;
+        custom() {
+            // Custom UI not supported in RPC mode.
+            // Throw synchronously (not a rejected promise): extensions commonly wrap
+            // custom() in `new Promise((resolve) => { ctx.ui.custom((...) => { ... resolve(); }) })`.
+            // A silent no-op hangs them forever (renderFn is never invoked); a rejected
+            // promise would be unhandled since the return value is ignored. A sync throw
+            // inside the Promise executor rejects that wrapper promise, so the command
+            // handler fails fast with a clear error instead of deadlocking the session.
+            // The PI_TUI_ONLY code lets the command dispatcher recognize this case and
+            // degrade the slash command to a plain LLM prompt instead of an error.
+            const err = new Error("ctx.ui.custom() is not supported in RPC mode (interactive TUI panels are unavailable)");
+            err.code = "PI_TUI_ONLY";
+            throw err;
         },
         pasteToEditor(text) {
             // Paste handling not supported in RPC mode - falls back to setEditorText
