# 企微机器人通讯录 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** 为企微机器人新增通讯录功能——自动收录对话过的人和群，在机器人管理界面查看并重命名（备注名），备注名联动 IM 会话列表。

**架构：** 新增独立 JSON 存储 `contacts.json`（仿 `channel-store.ts` 纯函数模式），在 `channel-manager.ts` 的 `handleInbound()` 进站时采集（单聊记人、群聊记群），通过 `routes/contacts.ts` + `ws-server.ts` 事件暴露 REST API，前端 `BotsSection` 内嵌通讯录滑出面板 + 行内展开重命名，`ImConversationList` 回显备注名。

**技术栈：** TypeScript、bun:test（kernel）、vitest + @testing-library/react（frontend）、Zustand、自研 HttpRouter + callApi 适配器。

**设计规格：** `docs/superpowers/specs/2026-08-14-contacts-design.md`

**命名说明：** 设计规格中的 `Contact` 在代码中落地为 `ContactEntity`（对齐 types.ts 现有 `ProjectEntity`/`SessionEntity` 的 `*Entity` 命名约定）。

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `packages/shared/src/constants.ts` | 修改 | 新增 `CONTACTS_FILE` 常量 |
| `packages/shared/src/types.ts` | 修改 | 新增 `ContactEntity`、`ContactsChangedEvent`，挂入 `WSServerEvent` 联合 |
| `packages/kernel/src/contact-store.ts` | 新建 | 通讯录 JSON 存储：`listContacts`/`upsertContact`/`renameContact` |
| `packages/kernel/src/channel-manager.ts` | 修改 | deps 加 `contactsFile`、`handleInbound` 采集、`listContacts`/`renameContact` 方法 |
| `packages/kernel/src/routes/contacts.ts` | 新建 | `GET /api/contacts`、`PUT /api/contacts/:id` 路由 |
| `packages/kernel/src/ws-server.ts` | 修改 | 注册路由 + `contacts:list`/`contacts:rename` 事件处理 + 广播 |
| `packages/kernel/tests/contact-store.test.ts` | 新建 | contact-store 单元测试 |
| `packages/kernel/tests/routes-contacts.test.ts` | 新建 | 路由 + callApi 映射测试 |
| `packages/frontend/src/store/contacts.ts` | 新建 | Zustand store（拉全部 + 重命名） |
| `packages/frontend/src/components/settings/ContactsPanel.tsx` | 新建 | 通讯录滑出面板 + 行内展开重命名 |
| `packages/frontend/src/components/settings/BotsSection.tsx` | 修改 | 「通讯录」入口 + 渲染面板 |
| `packages/frontend/src/components/ImConversationList.tsx` | 修改 | `titleOf` 回显备注名 |
| `packages/frontend/src/App.tsx` | 修改 | `contacts:changed` SSE 刷新 |
| `packages/frontend/src/components/settings/ContactsPanel.test.tsx` | 新建 | 面板组件测试 |

---

## 任务 1：shared 类型 + 常量

**文件：**
- 修改：`packages/shared/src/constants.ts`（第 49 行 `EXECUTION_RECORDS_FILE` 之后）
- 修改：`packages/shared/src/types.ts`（第 616 行 `ChannelConversationInfo` 之后；第 661 行 `ChannelConversationsChangedEvent` 之后；第 1334 行 `| ChannelConversationsChangedEvent` 之后）

- [ ] **步骤 1：加 `CONTACTS_FILE` 常量**

`packages/shared/src/constants.ts`，在 `EXECUTION_RECORDS_FILE` 那行之后加：

```ts
export const CONTACTS_FILE = `${WA_PI_DIR}/contacts.json`; // 企微机器人通讯录（对话过的人/群）
```

- [ ] **步骤 2：加 `ContactEntity` 接口**

`packages/shared/src/types.ts`，在 `ChannelConversationInfo` 接口（第 616 行结束）之后加：

```ts
/** 通讯录条目：一个机器人下对话过的人（person）或群（group）。kind=person 用 userId，kind=group 用 chatId */
export interface ContactEntity {
	id: string; // ct_xxx
	channelId: string; // 所属机器人 ch_xxx
	kind: "person" | "group";
	userId?: string; // kind=person：企微 userid（单聊的 fromUserId）
	chatId?: string; // kind=group：群 chatid
	remark?: string; // 备注名（用户重命名结果）
	firstChatAt: number; // 首次对话时间戳 ms
	lastChatAt: number; // 最近对话时间戳 ms
}
```

- [ ] **步骤 3：加 `ContactsChangedEvent` 并挂入联合类型**

`packages/shared/src/types.ts`，在 `ChannelConversationsChangedEvent`（第 659 行）之后加：

```ts
export interface ContactsChangedEvent {
	type: "contacts:changed";
}
```

在第 1334 行 `| ChannelConversationsChangedEvent` 之后加一行：

```ts
	| ContactsChangedEvent
```

- [ ] **步骤 4：验证编译**

运行：`cd packages/shared && bun run typecheck`
预期：0 错误。

- [ ] **步骤 5：Commit**

```bash
git add packages/shared/src/constants.ts packages/shared/src/types.ts
git commit -m "feat(shared): 新增 ContactEntity 类型 + CONTACTS_FILE 常量"
```

---

## 任务 2：contact-store.ts 存储层

**文件：**
- 创建：`packages/kernel/src/contact-store.ts`
- 测试：`packages/kernel/tests/contact-store.test.ts`

- [ ] **步骤 1：编写失败的测试**

`packages/kernel/tests/contact-store.test.ts`：

```ts
import { test, expect, beforeEach, afterEach } from "bun:test";
import { mkdtempSync, rmSync, readFileSync } from "node:fs";
import { join } from "node:path";
import { tmpdir } from "node:os";
import {
	listContacts,
	renameContact,
	upsertContact,
} from "../src/contact-store";

let dir: string;
let file: string;
beforeEach(() => {
	dir = mkdtempSync(join(tmpdir(), "wa-pi-contact-"));
	file = join(dir, "contacts.json");
});
afterEach(() => rmSync(dir, { recursive: true, force: true }));

test("upsert 新建 person，按 channelId+userId 去重", async () => {
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file);
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file); // 重复
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u2" }, file);
	const list = await listContacts("ch_a", file);
	expect(list).toHaveLength(2); // u1 去重
	expect(list.map((c) => c.userId).sort()).toEqual(["u1", "u2"]);
});

test("upsert 更新 lastChatAt 但不覆盖 firstChatAt", async () => {
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file);
	const first = (await listContacts("ch_a", file))[0];
	await new Promise((r) => setTimeout(r, 5));
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file);
	const after = (await listContacts("ch_a", file))[0];
	expect(after.firstChatAt).toBe(first.firstChatAt);
	expect(after.lastChatAt).toBeGreaterThan(first.lastChatAt);
});

test("group 与 person 分维度，各自去重", async () => {
	await upsertContact({ channelId: "ch_a", kind: "group", chatId: "g1" }, file);
	await upsertContact({ channelId: "ch_a", kind: "group", chatId: "g1" }, file);
	await upsertContact({ channelId: "ch_a", kind: "group", chatId: "g2" }, file);
	const list = await listContacts("ch_a", file);
	expect(list.filter((c) => c.kind === "group")).toHaveLength(2);
});

test("listContacts 按 channelId 过滤 + lastChatAt 倒序", async () => {
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file);
	await upsertContact({ channelId: "ch_b", kind: "person", userId: "u1" }, file);
	const list = await listContacts("ch_a", file);
	expect(list).toHaveLength(1);
	expect(list[0].channelId).toBe("ch_a");
});

test("renameContact 设置 remark，不存在返回 null", async () => {
	await upsertContact({ channelId: "ch_a", kind: "person", userId: "u1" }, file);
	const c = (await listContacts("ch_a", file))[0];
	const renamed = await renameContact(c.id, "张三", file);
	expect(renamed?.remark).toBe("张三");
	expect(await renameContact("ct_notexist", "x", file)).toBeNull();
});

test("文件损坏/不存在 → 空列表不抛错", async () => {
	await import("node:fs/promises").then((fs) =>
		fs.writeFile(file, "{invalid json", "utf8"),
	);
	await expect(listContacts("ch_a", file)).resolves.toEqual([]);
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd packages/kernel && bun test tests/contact-store.test.ts`
预期：FAIL，报错无法导入 `../src/contact-store`。

- [ ] **步骤 3：实现 contact-store.ts**

`packages/kernel/src/contact-store.ts`：

```ts
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { dirname } from "node:path";
import { randomUUID } from "node:crypto";
import { CONTACTS_FILE, type ContactEntity } from "@wa-pi/shared";

async function readJson<T>(file: string, fallback: T): Promise<T> {
	try {
		return JSON.parse(await readFile(file, "utf8")) as T;
	} catch {
		return fallback; // 文件不存在/损坏 → 回退，不抛错
	}
}

async function writeJson(file: string, data: unknown): Promise<void> {
	await mkdir(dirname(file), { recursive: true });
	await writeFile(file, JSON.stringify(data, null, 2), "utf8");
}

/** 去重键：person 按 channelId+userId；group 按 channelId+chatId */
function dedupKey(
	c: Pick<ContactEntity, "channelId" | "kind" | "userId" | "chatId">,
): string {
	return c.kind === "group"
		? `${c.channelId}:group:${c.chatId}`
		: `${c.channelId}:person:${c.userId}`;
}

/** 某机器人的通讯录（channelId 空 = 全部），按 lastChatAt 倒序 */
export async function listContacts(
	channelId?: string,
	file: string = CONTACTS_FILE,
): Promise<ContactEntity[]> {
	const raw = await readJson<{ contacts?: ContactEntity[] }>(file, {});
	const list = Array.isArray(raw.contacts) ? raw.contacts : [];
	return list
		.filter((c) => !channelId || c.channelId === channelId)
		.sort((a, b) => b.lastChatAt - a.lastChatAt);
}

export type ContactUpsertInput =
	| { channelId: string; kind: "person"; userId: string }
	| { channelId: string; kind: "group"; chatId: string };

/** 新增或更新（去重键命中则更新 lastChatAt，否则新建带 firstChatAt） */
export async function upsertContact(
	input: ContactUpsertInput,
	file: string = CONTACTS_FILE,
): Promise<void> {
	const raw = await readJson<{ contacts?: ContactEntity[] }>(file, {});
	const contacts = Array.isArray(raw.contacts) ? raw.contacts : [];
	const key = dedupKey(input);
	const now = Date.now();
	const existing = contacts.find((c) => dedupKey(c) === key);
	if (existing) {
		existing.lastChatAt = now;
	} else {
		contacts.push({
			id: `ct_${randomUUID().slice(0, 8)}`,
			channelId: input.channelId,
			kind: input.kind,
			userId: input.kind === "person" ? input.userId : undefined,
			chatId: input.kind === "group" ? input.chatId : undefined,
			firstChatAt: now,
			lastChatAt: now,
		});
	}
	await writeJson(file, { schemaVersion: 1, contacts });
}

/** 重命名；id 不存在返回 null */
export async function renameContact(
	id: string,
	remark: string,
	file: string = CONTACTS_FILE,
): Promise<ContactEntity | null> {
	const raw = await readJson<{ contacts?: ContactEntity[] }>(file, {});
	const contacts = Array.isArray(raw.contacts) ? raw.contacts : [];
	const c = contacts.find((x) => x.id === id);
	if (!c) return null;
	c.remark = remark;
	await writeJson(file, { schemaVersion: 1, contacts });
	return c;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd packages/kernel && bun test tests/contact-store.test.ts`
预期：PASS，7 个用例全绿。

- [ ] **步骤 5：Commit**

```bash
git add packages/kernel/src/contact-store.ts packages/kernel/tests/contact-store.test.ts
git commit -m "feat(kernel): 通讯录存储层 contact-store（list/upsert/rename）"
```

---

## 任务 3：channel-manager 采集 + 访问方法

**文件：**
- 修改：`packages/kernel/src/channel-manager.ts`
- 测试：`packages/kernel/tests/channel-manager.test.ts`（追加）

- [ ] **步骤 1：编写失败的测试（追加到 channel-manager.test.ts）**

在 `packages/kernel/tests/channel-manager.test.ts` 追加（先读现有文件的 mock deps 构造方式，复用其 `new ChannelManager({...})` 写法）：

```ts
import {
	listContacts,
} from "../src/contact-store";

test("handleInbound 采集：单聊记人、群聊记群", async () => {
	// 用临时 contactsFile 构造 ChannelManager（复用文件里现有的 mock deps，补 contactsFile）
	const dir = mkdtempSync(join(tmpdir(), "wa-pi-cm-contact-"));
	const contactsFile = join(dir, "contacts.json");
	// ... 复用现有测试里的 mock adapter + deps 构造，注入 contactsFile ...
	// 单聊进站
	await manager.handleInbound(channel, adapter, {
		chatId: "user-1", chatType: "single", fromUserId: "user-1",
		msgId: "m1", text: "hi", replyFrame: null,
	} as any);
	// 群聊进站
	await manager.handleInbound(channel, adapter, {
		chatId: "group-1", chatType: "group", fromUserId: "user-1",
		msgId: "m2", text: "hi", replyFrame: null,
	} as any);
	const contacts = await listContacts(channel.id, contactsFile);
	expect(contacts.some((c) => c.kind === "person" && c.userId === "user-1")).toBe(true);
	expect(contacts.some((c) => c.kind === "group" && c.chatId === "group-1")).toBe(true);
	rmSync(dir, { recursive: true, force: true });
});
```

> 注意：本测试的 mock deps 构造需对照现有 `channel-manager.test.ts` 里的既有写法（它已经 mock 了 `agentManager`/`broadcast` 等）。执行者先读该文件前 80 行找到构造样板，再补 `contactsFile` 字段。

- [ ] **步骤 2：运行测试验证失败**

运行：`cd packages/kernel && bun test tests/channel-manager.test.ts`
预期：新增用例 FAIL（`listContacts` 拿不到采集数据，因为采集逻辑还没写）。

- [ ] **步骤 3：修改 channel-manager.ts**

（1）顶部 import 区加：

```ts
import {
	listContacts as loadContacts,
	renameContact as renameContactById,
	upsertContact,
	type ContactEntity,
} from "./contact-store";
```

并在 `@wa-pi/shared` 的 import 里补 `CONTACTS_FILE`（若未导入）。

（2）`ChannelManagerDeps` 接口加字段（在 `mappingsFile?: string;` 之后）：

```ts
	contactsFile?: string;
```

（3）getter 区（`private get mappingsFile()` 之后）加：

```ts
	private get contactsFile() {
		return this.deps.contactsFile ?? CONTACTS_FILE;
	}
```

（4）`handleInbound()` 里，在 `this.lastFrames.set(key, msg.replyFrame);`（约第 518 行）之后、`const reply = ...` 之前，插入采集：

```ts
		// 采集通讯录：单聊记人、群聊记群（只记新对话，失败不阻断消息处理）
		try {
			if (msg.chatType === "group") {
				await upsertContact(
					{ channelId: channel.id, kind: "group", chatId: msg.chatId },
					this.contactsFile,
				);
			} else {
				await upsertContact(
					{ channelId: channel.id, kind: "person", userId: msg.fromUserId },
					this.contactsFile,
				);
			}
		} catch (e) {
			console.warn("[channel-manager] 通讯录采集失败:", e);
		}
```

（5）在类里加两个公开方法（放在 `listConversations` 附近）：

```ts
	async listContacts(channelId?: string): Promise<ContactEntity[]> {
		return loadContacts(channelId, this.contactsFile);
	}

	async renameContact(id: string, remark: string): Promise<ContactEntity | null> {
		return renameContactById(id, remark, this.contactsFile);
	}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd packages/kernel && bun test tests/channel-manager.test.ts`
预期：新增用例 PASS，既有用例不回归。

- [ ] **步骤 5：Commit**

```bash
git add packages/kernel/src/channel-manager.ts packages/kernel/tests/channel-manager.test.ts
git commit -m "feat(kernel): 进站采集通讯录 + ChannelManager 暴露 listContacts/renameContact"
```

---

## 任务 4：REST 路由 + ws-server 事件

**文件：**
- 创建：`packages/kernel/src/routes/contacts.ts`
- 修改：`packages/kernel/src/ws-server.ts`（import 区约 72 行、`registerRoutes()` 约 496 行、事件处理区约 2410 行）
- 测试：`packages/kernel/tests/routes-contacts.test.ts`

- [ ] **步骤 1：编写失败的测试**

`packages/kernel/tests/routes-contacts.test.ts`（用真实 HttpRouter + 捕获 callApi 入参，验证路由→callApi 映射）：

```ts
import { test, expect } from "bun:test";
import { HttpRouter } from "../src/http-router";
import { registerContactRoutes } from "../src/routes/contacts";
import type { WSClientEvent } from "@wa-pi/shared";

/** 起临时 HTTP 服务，捕获 callApi 收到的 event */
async function capture(path: string, init?: RequestInit) {
	const router = new HttpRouter();
	const calls: any[] = [];
	const callApi = async (e: WSClientEvent) => {
		calls.push(e);
		return Response.json({ ok: true });
	};
	registerContactRoutes(router, callApi as any, {} as any);
	const server = Bun.serve({
		port: 0,
		fetch: async (req) => (await router.handle(req)) ?? new Response("nf", { status: 404 }),
	});
	try {
		await fetch(`http://localhost:${server.port}${path}`, {
			...init,
			headers: { "Content-Type": "application/json", ...(init?.headers ?? {}) },
		});
	} finally {
		server.stop();
	}
	return calls[0];
}

test("GET /api/contacts 带 channelId → contacts:list", async () => {
	const e = await capture("/api/contacts?channelId=ch_abc");
	expect(e.type).toBe("contacts:list");
	expect(e.channelId).toBe("ch_abc");
});

test("PUT /api/contacts/:id → contacts:rename", async () => {
	const e = await capture("/api/contacts/ct_123", {
		method: "PUT",
		body: JSON.stringify({ remark: "张三" }),
	});
	expect(e.type).toBe("contacts:rename");
	expect(e.id).toBe("ct_123");
	expect(e.remark).toBe("张三");
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd packages/kernel && bun test tests/routes-contacts.test.ts`
预期：FAIL（无法导入 `../src/routes/contacts`）。

- [ ] **步骤 3：实现 routes/contacts.ts**

`packages/kernel/src/routes/contacts.ts`：

```ts
/**
 * 企微机器人通讯录域路由（阶段二·去 WS 化）
 * 仿 channels.ts：GET 列表 / PUT 重命名，经 callApi 复用 ws-server 的 handle 业务逻辑。
 */
import type { RouteRegistrar } from "./types";
import { readJsonBody } from "./types";

export const registerContactRoutes: RouteRegistrar = (r, callApi) => {
	// 某机器人的通讯录（channelId 空 = 全部）
	r.add("GET", "/api/contacts", async (req) => {
		const channelId = new URL(req.url).searchParams.get("channelId") ?? "";
		return callApi({ type: "contacts:list", channelId });
	});
	// 重命名
	r.add("PUT", "/api/contacts/:id", async (req, p) => {
		const b = await readJsonBody(req);
		return callApi({ type: "contacts:rename", id: p.id, remark: b.remark });
	});
};
```

- [ ] **步骤 4：修改 ws-server.ts**

（1）import 区（`registerChannelRoutes` 之后）加：

```ts
import { registerContactRoutes } from "./routes/contacts";
```

（2）`registerRoutes()` 里（`registerChannelRoutes(this.router, callApi, ctx);` 之后）加：

```ts
		registerContactRoutes(this.router, callApi, ctx);
```

（3）事件处理区（`channel-conversations:list` case 之后，约 2410 行）加：

```ts
			case "contacts:list": {
				const contacts = this.opts.channelManager
					? await this.opts.channelManager.listContacts(event.channelId || undefined)
					: [];
				reply({ type: "contacts:current", contacts });
				break;
			}
			case "contacts:rename": {
				const c = await this.opts.channelManager!.renameContact(
					event.id,
					event.remark,
				);
				if (!c) {
					reply({ type: "error", message: "联系人不存在", status: 404 });
					break;
				}
				this.broadcast({ type: "contacts:changed" });
				reply({
					type: "contacts:current",
					contacts: await this.opts.channelManager!.listContacts(c.channelId),
				});
				break;
			}
```

- [ ] **步骤 5：运行测试验证通过**

运行：`cd packages/kernel && bun test tests/routes-contacts.test.ts`
预期：PASS。再跑 `bun test` 全量确认无回归（重点看 ws-server 相关测试）。

- [ ] **步骤 6：Commit**

```bash
git add packages/kernel/src/routes/contacts.ts packages/kernel/src/ws-server.ts packages/kernel/tests/routes-contacts.test.ts
git commit -m "feat(kernel): 通讯录 REST 路由 + ws-server 事件 + 广播"
```

---

## 任务 5：前端 store/contacts.ts

**文件：**
- 创建：`packages/frontend/src/store/contacts.ts`

- [ ] **步骤 1：创建 store**

`packages/frontend/src/store/contacts.ts`（仿 `store/channels.ts`）：

```ts
import { create } from "zustand";
import { api } from "../api-client";
import type { ContactEntity } from "@wa-pi/shared";

interface ContactsState {
	contacts: ContactEntity[];
	loadContacts: () => Promise<void>;
	renameContact: (id: string, remark: string) => Promise<void>;
}

export const useContactsStore = create<ContactsState>((set) => ({
	contacts: [],
	loadContacts: async () => {
		const res = (await api.get("/api/contacts")) as any;
		set({ contacts: res?.contacts ?? [] });
	},
	renameContact: async (id, remark) => {
		const res = (await api.put(`/api/contacts/${id}`, { remark })) as any;
		set({ contacts: res?.contacts ?? [] });
	},
}));

/** 按 channelId + kind + id 查备注名（供 ImConversationList 回显） */
export function remarkOf(
	contacts: ContactEntity[],
	channelId: string,
	kind: "person" | "group",
	key: string,
): string | undefined {
	return contacts.find(
		(c) =>
			c.channelId === channelId &&
			c.kind === kind &&
			(kind === "group" ? c.chatId === key : c.userId === key),
	)?.remark;
}
```

- [ ] **步骤 2：验证编译**

运行：`cd packages/frontend && bun run typecheck`
预期：0 错误。

- [ ] **步骤 3：Commit**

```bash
git add packages/frontend/src/store/contacts.ts
git commit -m "feat(frontend): 通讯录 Zustand store + remarkOf 备注名查找"
```

---

## 任务 6：ContactsPanel 组件 + BotsSection 集成

**文件：**
- 创建：`packages/frontend/src/components/settings/ContactsPanel.tsx`
- 修改：`packages/frontend/src/components/settings/BotsSection.tsx`
- 测试：`packages/frontend/src/components/settings/ContactsPanel.test.tsx`

- [ ] **步骤 1：编写失败的组件测试**

`packages/frontend/src/components/settings/ContactsPanel.test.tsx`（mock `useContactsStore`，断言渲染 + 行内展开重命名）：

```tsx
import { test, expect, vi, beforeEach } from "vitest";
import { render, screen, fireEvent } from "@testing-library/react";
import ContactsPanel from "./ContactsPanel";

// mock store：返回固定 contacts + renameContact
const renameContact = vi.fn();
vi.mock("../../store/contacts", () => ({
	useContactsStore: () => ({
		contacts: [
			{ id: "ct_1", channelId: "ch_a", kind: "person", userId: "u1", firstChatAt: 1, lastChatAt: 2 },
			{ id: "ct_2", channelId: "ch_a", kind: "group", chatId: "g1", firstChatAt: 1, lastChatAt: 2 },
		],
		renameContact,
		loadContacts: vi.fn(),
	}),
}));

beforeEach(() => renameContact.mockReset());

test("渲染人/群两类列表", () => {
	render(<ContactsPanel channelId="ch_a" onClose={() => {}} />);
	expect(screen.getByText(/人/)).toBeTruthy();
	expect(screen.getByText(/群/)).toBeTruthy();
	expect(screen.getByText("u1")).toBeTruthy();
});

test("点行展开输入框，保存调用 renameContact", async () => {
	render(<ContactsPanel channelId="ch_a" onClose={() => {}} />);
	fireEvent.click(screen.getByText("u1"));
	const input = screen.getByRole("textbox");
	fireEvent.change(input, { target: { value: "张三" } });
	fireEvent.click(screen.getByText("保存"));
	expect(renameContact).toHaveBeenCalledWith("ct_1", "张三");
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd packages/frontend && bun --env-file=.env.test test --isolate src/components/settings/ContactsPanel.test.tsx`
预期：FAIL（无法导入 ContactsPanel）。

- [ ] **步骤 3：实现 ContactsPanel.tsx**

`packages/frontend/src/components/settings/ContactsPanel.tsx`：

```tsx
import { useMemo, useState } from "react";
import { useContactsStore } from "../../store/contacts";
import { useToastStore } from "../../store/toast";
import type { ContactEntity } from "@wa-pi/shared";

/** 通讯录滑出面板：人/群两类 + 行内展开重命名 */
export default function ContactsPanel({
	channelId,
	onClose,
}: {
	channelId: string;
	onClose: () => void;
}) {
	const contacts = useContactsStore((s) => s.contacts);
	const { renameContact, loadContacts } = useContactsStore.getState();
	const [editingId, setEditingId] = useState<string | null>(null);
	const [value, setValue] = useState("");

	const { persons, groups } = useMemo(() => {
		const mine = contacts.filter((c) => c.channelId === channelId);
		return {
			persons: mine.filter((c) => c.kind === "person"),
			groups: mine.filter((c) => c.kind === "group"),
		};
	}, [contacts, channelId]);

	const label = (c: ContactEntity) =>
		c.remark || (c.kind === "group" ? (c.chatId ?? "").slice(0, 8) : c.userId);

	const save = async (c: ContactEntity) => {
		try {
			await renameContact(c.id, value.trim());
			setEditingId(null);
		} catch (e) {
			useToastStore.getState().add(e instanceof Error ? e.message : String(e), "error");
		}
	};

	return (
		<div className="w-64 border-l border-hairline flex flex-col" data-testid="contacts-panel">
			<div className="flex items-center justify-between px-3 py-2 border-b border-hairline">
				<span className="text-sm text-primary">通讯录</span>
				<button onClick={onClose} className="text-tertiary cursor-pointer" data-testid="contacts-close">✕</button>
			</div>
			<div className="flex-1 overflow-auto p-3 flex flex-col gap-1">
				{persons.length === 0 && groups.length === 0 && (
					<div className="text-xs text-tertiary text-center py-4">暂无对话过的人/群</div>
				)}
				{persons.length > 0 && <div className="text-xs text-secondary font-medium">人</div>}
				{persons.map((c) => (
					<div key={c.id} className="border border-hairline rounded-sm">
						<div
							className="flex items-center justify-between px-2 py-1.5 cursor-pointer"
							onClick={() => { setEditingId(c.id); setValue(c.remark ?? ""); }}
						>
							<span className="text-sm text-primary">{label(c)}</span>
							<span className="text-xs text-tertiary">⋯</span>
						</div>
						{editingId === c.id && (
							<div className="flex gap-1 px-2 pb-2">
								<input
									autoFocus
									value={value}
									onChange={(e) => setValue(e.target.value)}
									onKeyDown={(e) => e.key === "Enter" && void save(c)}
									className="flex-1 px-2 py-1 rounded-sm border border-hairline bg-surface text-sm text-primary outline-none"
								/>
								<button onClick={() => void save(c)} className="px-2 py-1 rounded-sm text-xs" style={{ background: "var(--brand)", color: "var(--on-brand)" }}>保存</button>
								<button onClick={() => setEditingId(null)} className="px-2 py-1 rounded-sm text-xs border border-hairline">取消</button>
							</div>
						)}
					</div>
				))}
				{groups.length > 0 && <div className="text-xs text-secondary font-medium mt-2">群</div>}
				{groups.map((c) => (
					<div key={c.id} className="border border-hairline rounded-sm">
						<div
							className="flex items-center justify-between px-2 py-1.5 cursor-pointer"
							onClick={() => { setEditingId(c.id); setValue(c.remark ?? ""); }}
						>
							<span className="text-sm text-primary">{label(c)}</span>
							<span className="text-xs text-tertiary">⋯</span>
						</div>
						{editingId === c.id && (
							<div className="flex gap-1 px-2 pb-2">
								<input
									autoFocus
									value={value}
									onChange={(e) => setValue(e.target.value)}
									onKeyDown={(e) => e.key === "Enter" && void save(c)}
									className="flex-1 px-2 py-1 rounded-sm border border-hairline bg-surface text-sm text-primary outline-none"
								/>
								<button onClick={() => void save(c)} className="px-2 py-1 rounded-sm text-xs" style={{ background: "var(--brand)", color: "var(--on-brand)" }}>保存</button>
								<button onClick={() => setEditingId(null)} className="px-2 py-1 rounded-sm text-xs border border-hairline">取消</button>
							</div>
						)}
					</div>
				))}
			</div>
		</div>
	);
}
```

- [ ] **步骤 4：修改 BotsSection.tsx**

（1）state 区（`confirmDelete` 之后）加：

```tsx
	const [contactsOpen, setContactsOpen] = useState(false);
```

（2）右栏编辑表单顶部（`draft` 非空时的表单开头，约 L155 附近）加「通讯录」按钮：

```tsx
	<button
		onClick={() => setContactsOpen(true)}
		className="self-end px-3 py-1.5 rounded-sm text-sm border border-hairline cursor-pointer"
		data-testid="bot-contacts-btn"
	>
		通讯录
	</button>
```

（3）最外层容器末尾（`confirmDelete` 块之后，约 L250）加面板渲染：

```tsx
	{contactsOpen && selectedId && (
		<ContactsPanel channelId={selectedId} onClose={() => setContactsOpen(false)} />
	)}
```

（4）顶部 import 加 `import ContactsPanel from "./ContactsPanel";`

- [ ] **步骤 5：运行测试验证通过**

运行：`cd packages/frontend && bun --env-file=.env.test test --isolate src/components/settings/ContactsPanel.test.tsx`
预期：PASS。再跑 BotsSection 相关既有测试确认无回归。

- [ ] **步骤 6：Commit**

```bash
git add packages/frontend/src/components/settings/ContactsPanel.tsx packages/frontend/src/components/settings/BotsSection.tsx packages/frontend/src/components/settings/ContactsPanel.test.tsx
git commit -m "feat(frontend): 通讯录滑出面板 + 行内展开重命名 + BotsSection 入口"
```

---

## 任务 7：IM 会话列表备注名联动 + SSE 刷新

**文件：**
- 修改：`packages/frontend/src/components/ImConversationList.tsx`
- 修改：`packages/frontend/src/App.tsx`

- [ ] **步骤 1：修改 ImConversationList.titleOf**

（1）顶部 import 加 `import { useContactsStore } from "../store/contacts";`

（2）`titleOf` 改为备注名优先：

```tsx
	/** 列表项标题：单聊显示备注名(回退 userid)；群聊显示备注名(回退 群聊(chatId前8)·发送者) */
	const contacts = useContactsStore((s) => s.contacts);
	const titleOf = (c: ChannelConversationInfo) => {
		const remark = contacts.find(
			(x) =>
				x.channelId === c.channelId &&
				(c.chatType === "group"
					? x.kind === "group" && x.chatId === c.chatId
					: x.kind === "person" && x.userId === c.fromUserId),
		)?.remark;
		if (remark) return remark;
		return c.chatType === "group"
			? t("im.groupTitle", { chatId: c.chatId.slice(0, 8), from: c.fromUserId })
			: c.chatId;
	};
```

- [ ] **步骤 2：修改 App.tsx 加 SSE 刷新**

（1）顶部 import 加 `import { useContactsStore } from "./store/contacts";`

（2）`onMessage` 的 switch（`channel-conversations:changed` case 之后，约 L277）加：

```tsx
				case "contacts:changed":
					void useContactsStore.getState().loadContacts();
					break;
```

（3）在「首次挂载加载」的 effect（约 L199 附近）里，`loadBots`/`loadConversations` 调用处补一句 `void useContactsStore.getState().loadContacts();`（确保启动即拉取，供 ImConversationList 回显）。

- [ ] **步骤 3：验证编译 + 跑相关测试**

运行：`cd packages/frontend && bun run typecheck`
运行：`cd packages/frontend && bun --env-file=.env.test test --isolate src/components/ImConversationList.test.tsx`（若存在该测试文件）
预期：0 错误，测试通过。

- [ ] **步骤 4：全量测试**

运行（根目录）：`bun run test`
预期：kernel / shared / desktop / frontend 全量通过，无回归。

- [ ] **步骤 5：Commit**

```bash
git add packages/frontend/src/components/ImConversationList.tsx packages/frontend/src/App.tsx
git commit -m "feat(frontend): IM 会话列表备注名回显 + contacts:changed SSE 刷新"
```

---

## 自检结果

- **规格覆盖**：数据模型（任务 1/2）、存储（2）、采集（3）、API（4）、前端面板 + 重命名（5/6）、IM 联动 + SSE（7）、错误处理（contact-store 兜底 + rename 404 已覆盖）均已映射到任务。✅
- **占位符扫描**：无「TODO/待定」；每个代码步骤都有实际代码块。任务 3 的测试 mock 有一处「复用现有构造样板」的说明，属对既有测试文件的正当引用，非占位。✅
- **类型一致性**：`ContactEntity` 字段（id/channelId/kind/userId/chatId/remark/firstChatAt/lastChatAt）在 contact-store、channel-manager、前端 store、ContactsPanel 全程一致；事件名 `contacts:list`/`contacts:rename`/`contacts:changed`/`contacts:current` 前后一致。✅
