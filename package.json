{
  "name": "hiagent",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "dev:kernel": "bun run --filter hiagent-kernel dev",
    "dev:frontend": "bun run --filter hiagent-frontend dev"
  }
}
